package main

// regex match strings

// veloq needs to match the output of commands for regex matches

// a command for example could output the following things: WARNING, ERROR, timeout, 100%, loading

// this file demonstrates how to regex match against strings in go and
// defines some sensitive defaults for later use

// it would be nice to also get back the exact places of matched characters to hightlight them later

// this could be part of the util package later

// general matching against regex
func match(str, regex string) bool {
	return false
}
